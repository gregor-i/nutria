//package meta
//
//import entities.fractal.Mandelbrot.Cardroid
//
//object CardroidTest extends App{
//  val c = Cardroid(500, 100)
//
////  for(x <- 0d to 1d by 0.1)
////    for(y <- 0d to 1d by 0.1)
////      for(t <- 0d to Math.PI*2 by 0.1)
////        println(x, y, t, c.dist(x, y)(t))
//  for(x <- -1d to 1d by 0.1)
//   println(x, c.dist(0, x))
//}
